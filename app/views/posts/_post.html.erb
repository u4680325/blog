<div id="<%= dom_id post %>">
  <div>
    <strong>Title:</strong>
    <%= post.title %>
  </div>

  <div>
    <strong>Body:</strong>
    <%= post.body %>
  </div>

  <div>
    <%= time_tag post.created_at, "data-local": "time", "data-format": "%d-%m-%Y %H:%M" %> <strong>created</strong> | <%= time_tag post.updated_at, "data-local": "time", "data-format": "%d-%m-%Y %H:%M" %> <strong>updated</strong>
  </div>
</div>
